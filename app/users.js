/*
This file contains the model to store the userdata.
*/

// Don't need this now
/*

var mongoose = require('mongoose');
var bcrypt = require('bcrypt');

//Schema the database will be using.
var Schema = mongoose.Schema({

    local: {
        username: String,
        email: String,
        password: String,
    }

});

module.exports = mongoose.model('users', Schema);

*/